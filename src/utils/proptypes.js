import PropTypes from "prop-types";

export const CompanyPropType = {
  name: PropTypes.string,
  firstPurchaseDate: PropTypes.string,
  totalBudget: PropTypes.number,
  budgetSpent: PropTypes.number,
  budgetLeft: PropTypes.number
};
